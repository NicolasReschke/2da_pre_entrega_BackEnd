<h2>Perfil de Usuario</h2>

<div class="card" style="width: 28rem;">
    <img src="{{user.profile_image}}" class="card-img-top">
    <div class="card-body">
        <h5 class="card-title">{{user.first_name}} {{user.last_name}}</h5>
        <p class="card-text"><strong>ID: </strong>{{user._id}} <br><strong>Cart ID: </strong>{{user.cart._id}}</p>
    </div>
    <ul class="list-group list-group-flush">
        <li class="list-group-item"><strong>Email: </strong>{{user.email}}</li>
        <li class="list-group-item"><strong>Edad: </strong>{{user.age}}</li>
        <li class="list-group-item">{{user.role}}</li>
    </ul>
    <div class="card-body">
        <a href="/profile/:uid"><button class="btn btn-success">Editar mi perfil</button></a>
        <br>
        <br>
        <a href="/profile/:uid"><button class="btn btn-danger">Eliminar mi perfil</button></a>
    </div>
</div>